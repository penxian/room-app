---
description: 
globs: 
alwaysApply: false
---
# 认证系统指南

## 认证流程
1. 用户通过 [app/login](mdc:app/login) 页面进行登录
2. 认证状态通过 [middleware.ts](mdc:middleware.ts) 进行全局管理
3. 受保护的路由在 [app/private](mdc:app/private) 目录下

## 关键文件
- 登录页面：[app/login/page.tsx](mdc:app/login/page.tsx)
- 认证中间件：[middleware.ts](mdc:middleware.ts)
- 认证相关 API：[app/api/auth](mdc:app/api/auth)

## 开发注意事项
1. 所有受保护的路由必须放在 `app/private` 目录下
2. 使用 Supabase 的 SSR 功能进行认证
3. 确保在开发时正确配置环境变量

## 安全建议
1. 始终使用 HTTPS
2. 实现适当的 CSRF 保护
3. 使用安全的密码策略
4. 实现适当的会话管理

# 认证系统与 API 设计规范

## 目录结构
- `app/api/auth/`：所有公开认证相关接口（如登录、注册、忘记密码、邮箱验证等）
- `app/api/user/`：所有需要登录态的用户相关接口（如获取/更新用户信息、退出登录等）
- `app/api/notes/`：所有需要登录态的业务接口（如笔记增删改查）

## 鉴权规则
- 仅 `app/api/user/*`、`app/api/notes/*` 等接口需要登录态
- `app/api/auth/*` 下所有接口为公开接口，不做登录校验

## 中间件配置
- `middleware.ts` 只拦截需要鉴权的 API 路由和页面路由
- 公开 API 路由和公开页面不拦截

## 其它建议
- 禁止使用 server action，所有服务端逻辑通过 RESTful API 实现
- 所有 API 路由必须有明确的分层和注释

